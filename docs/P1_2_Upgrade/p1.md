# Sprint P1 & P2 — Complete Documentation Pack

---

# 📄 01_P1_Implementation_Pack.md

```markdown
# P1 Implementation Pack
# Sprint: Indicator Table + Enhanced Rule Engine + Snapshot Normalization
# Version: 1.0
# Date: 2026-08-10
# Prerequisites: P0 Complete ✅

---

## 1. SPRINT OVERVIEW

### 1.1 Mục Tiêu P1

Sau P0, hệ thống đã có:
✅ Weighted Narrative Health
✅ Rule Version Tracking
✅ Health Timeline UI
✅ Technical Analysis fixes

P1 giải quyết:

| ID  | Vấn đề                              | Impact |
|-----|-------------------------------------|--------|
| P1A | Indicator Table (tách khỏi Feature) | HIGH   |
| P1B | Multi-condition Rule Engine         | HIGH   |
| P1C | Morning Snapshot Normalization      | MEDIUM |
| P1D | Feature Provenance Tracking         | MEDIUM |
| P1E | Analyst Imports Fix (analyzer.ts)   | LOW    |

### 1.2 Định Nghĩa Done

- [ ] Indicator table tồn tại, data được lưu từ refresh
- [ ] Rule Engine hỗ trợ multi-condition AND/OR
- [ ] Morning Snapshot có 3 normalized tables
- [ ] Feature ghi lại source provenance
- [ ] analyzer.ts import đúng types
- [ ] Review Pack pass
- [ ] Verification Pack pass

### 1.3 Out of Scope P1

- Decision Engine Layer (P2)
- Event Engine (Phase 2)
- ML/AI (Phase 3)
- Real-time WebSocket (Technical Improvement)

---

## 2. KIẾN TRÚC THAY ĐỔI P1

### 2.1 Trước P1

```
Data Collection
      ↓
Feature Engine (indicators embedded in JSON)
      ↓
Health Score (rule: single threshold)
      ↓
Morning Snapshot (1 table, JSON blobs)
```

### 2.2 Sau P1

```
Data Collection
      ↓
Indicator Engine ──→ indicators table (EMA, RSI, ADX, OI, Funding...)
      ↓
Feature Engine (reads from indicators table)
  + Provenance tracking (which source, which version)
      ↓
Health Score
      ↓
Rule Engine v2 (multi-condition AND/OR rules, from DB)
      ↓
Recommendation
      ↓
Morning Snapshot (3 normalized tables: header + coins + narratives)
```

### 2.3 Database Changes P1

#### Bảng mới: `indicators`

```sql
CREATE TABLE indicators (
  id              SERIAL PRIMARY KEY,
  coin_id         INTEGER NOT NULL REFERENCES coins(id),
  date            DATE NOT NULL,
  timeframe       VARCHAR(10) NOT NULL,   -- '1d', '4h', '1h', '15m'
  indicator_type  VARCHAR(50) NOT NULL,   -- 'EMA_9', 'RSI_14', 'MACD', etc.
  indicator_value DECIMAL(20, 8),
  indicator_meta  JSONB,                  -- extra values: signal, histogram
  source          VARCHAR(30),            -- 'binance_spot', 'binance_futures'
  calculated_at   TIMESTAMP NOT NULL DEFAULT NOW(),
  UNIQUE(coin_id, date, timeframe, indicator_type)
);
```

#### Bảng mới: `recommendation_rules`

```sql
CREATE TABLE recommendation_rules (
  id             SERIAL PRIMARY KEY,
  rule_version_id INTEGER NOT NULL REFERENCES rule_versions(id),
  priority       INTEGER NOT NULL,
  signal         VARCHAR(20) NOT NULL,
  logic_operator VARCHAR(5) NOT NULL DEFAULT 'AND', -- 'AND' | 'OR'
  conditions     JSONB NOT NULL,
  reason_template TEXT NOT NULL,
  is_active      BOOLEAN NOT NULL DEFAULT TRUE,
  created_at     TIMESTAMP DEFAULT NOW()
);
```

#### Alter: `features` — thêm provenance

```sql
ALTER TABLE features
  ADD COLUMN IF NOT EXISTS source_provenance JSONB,
  ADD COLUMN IF NOT EXISTS calculated_at     TIMESTAMP;
```

#### Bảng mới: `morning_snapshots_v2` (normalized)

```sql
CREATE TABLE morning_snapshot_headers (
  id              SERIAL PRIMARY KEY,
  date            DATE NOT NULL UNIQUE,
  total_coins     INTEGER,
  avg_health_score DECIMAL(5,2),
  top_narrative_id INTEGER REFERENCES narratives(id),
  alert_count     INTEGER DEFAULT 0,
  rule_version_id INTEGER REFERENCES rule_versions(id),
  timezone        VARCHAR(50) DEFAULT 'Asia/Ho_Chi_Minh',
  created_at      TIMESTAMP DEFAULT NOW()
);

CREATE TABLE morning_snapshot_coins (
  id              SERIAL PRIMARY KEY,
  snapshot_id     INTEGER NOT NULL
    REFERENCES morning_snapshot_headers(id) ON DELETE CASCADE,
  coin_id         INTEGER NOT NULL REFERENCES coins(id),
  health_score    DECIMAL(5,2),
  score_change    DECIMAL(5,2),
  signal          VARCHAR(20),
  confidence      DECIMAL(5,2),
  UNIQUE(snapshot_id, coin_id)
);

CREATE TABLE morning_snapshot_narratives (
  id               SERIAL PRIMARY KEY,
  snapshot_id      INTEGER NOT NULL
    REFERENCES morning_snapshot_headers(id) ON DELETE CASCADE,
  narrative_id     INTEGER NOT NULL REFERENCES narratives(id),
  health_score     DECIMAL(5,2),
  score_change     DECIMAL(5,2),
  coin_count       INTEGER,
  top_coin_id      INTEGER REFERENCES coins(id),
  weakest_coin_id  INTEGER REFERENCES coins(id),
  weighting_method VARCHAR(20),
  UNIQUE(snapshot_id, narrative_id)
);
```

---

## 3. INDICATOR ENGINE (P1A)

### 3.1 Indicator Types Registry

```typescript
// src/lib/indicators/registry.ts

export const INDICATOR_TYPES = {
  // Price-based
  EMA_9:    { name: 'EMA_9',    timeframes: ['1d','4h','1h'], category: 'trend' },
  EMA_21:   { name: 'EMA_21',   timeframes: ['1d','4h','1h'], category: 'trend' },
  EMA_50:   { name: 'EMA_50',   timeframes: ['1d','4h'],      category: 'trend' },
  EMA_200:  { name: 'EMA_200',  timeframes: ['1d'],           category: 'trend' },
  RSI_14:   { name: 'RSI_14',   timeframes: ['1d','4h','1h'], category: 'momentum' },
  MACD:     { name: 'MACD',     timeframes: ['1d','4h'],      category: 'momentum' },
  ADX_14:   { name: 'ADX_14',   timeframes: ['1d','4h'],      category: 'trend' },
  BB_20:    { name: 'BB_20',    timeframes: ['1d','4h'],      category: 'volatility' },
  ATR_14:   { name: 'ATR_14',   timeframes: ['1d','4h'],      category: 'volatility' },
  VWAP_20:  { name: 'VWAP_20',  timeframes: ['4h','1h'],      category: 'volume' },

  // Derivatives
  OI_CHANGE:    { name: 'OI_CHANGE',    timeframes: ['1d'], category: 'derivative' },
  FUNDING_RATE: { name: 'FUNDING_RATE', timeframes: ['1d'], category: 'derivative' },
  LIQUIDATION:  { name: 'LIQUIDATION',  timeframes: ['1d'], category: 'derivative' },

  // Volume
  VOLUME_RATIO: { name: 'VOLUME_RATIO', timeframes: ['1d','4h'], category: 'volume' },
  OBV:          { name: 'OBV',          timeframes: ['1d'],      category: 'volume' },
} as const;

export type IndicatorType = keyof typeof INDICATOR_TYPES;
```

### 3.2 Indicator Calculation Flow

```
Kline Data (OHLCV)
      ↓
calculateIndicators(klines, date, timeframe)
      ↓
Save to indicators table
      ↓
Feature Engine reads indicators WHERE coin_id AND date
```

### 3.3 Feature Provenance Format

```typescript
interface FeatureProvenance {
  trend: {
    sources: string[];       // ['binance_futures', 'binance_spot']
    indicators: string[];    // ['EMA_9', 'EMA_21', 'EMA_50', 'ADX_14']
    calculated_at: string;   // ISO timestamp
    confidence: number;      // 0-1
  };
  derivative: {
    sources: string[];       // ['binance_futures']
    indicators: string[];    // ['OI_CHANGE', 'FUNDING_RATE']
    calculated_at: string;
    confidence: number;
    missing: string[];       // ['LIQUIDATION'] if not available
  };
  volume: { ... };
  momentum: { ... };
}
```

---

## 4. RULE ENGINE v2 (P1B)

### 4.1 Rule Structure

```typescript
interface RecommendationRule {
  id:             number;
  ruleVersionId:  number;
  priority:       number;     // Higher = checked first
  signal:         string;     // 'STRONG_WATCH', 'WATCH', 'OBSERVE', 'WEAK'
  logicOperator:  'AND' | 'OR';
  conditions:     RuleCondition[];
  reasonTemplate: string;     // "Health {health} with strong trend {trend}"
  isActive:       boolean;
}

interface RuleCondition {
  field:    string;   // 'health', 'trend', 'derivative', 'volume', 'momentum', 'confidence'
  operator: '>' | '>=' | '<' | '<=' | '==' | '!=';
  value:    number;
}
```

### 4.2 Rule Evaluation Algorithm

```typescript
function evaluateRules(
  scores: ScoreInput,
  rules: RecommendationRule[]
): RecommendationResult {

  // Sort by priority DESC (highest first)
  const sorted = [...rules].sort((a, b) => b.priority - a.priority);

  for (const rule of sorted) {
    if (!rule.isActive) continue;

    const matches = rule.logicOperator === 'AND'
      ? rule.conditions.every(c => evaluateCondition(scores, c))
      : rule.conditions.some(c => evaluateCondition(scores, c));

    if (matches) {
      return {
        signal: rule.signal,
        reason: formatReason(rule.reasonTemplate, scores),
        ruleId: rule.id,
      };
    }
  }

  // Fallback
  return { signal: 'OBSERVE', reason: 'No matching rule', ruleId: null };
}

function evaluateCondition(scores: ScoreInput, cond: RuleCondition): boolean {
  const actual = scores[cond.field as keyof ScoreInput];
  if (actual === undefined || actual === null) return false;

  switch (cond.operator) {
    case '>':  return actual >  cond.value;
    case '>=': return actual >= cond.value;
    case '<':  return actual <  cond.value;
    case '<=': return actual <= cond.value;
    case '==': return actual === cond.value;
    case '!=': return actual !== cond.value;
    default:   return false;
  }
}
```

### 4.3 Default Rules Seed Data

```json
[
  {
    "priority": 100,
    "signal": "STRONG_WATCH",
    "logic_operator": "AND",
    "conditions": [
      {"field": "health",     "operator": ">=", "value": 85},
      {"field": "trend",      "operator": ">=", "value": 75},
      {"field": "derivative", "operator": ">=", "value": 70},
      {"field": "confidence", "operator": ">=", "value": 60}
    ],
    "reason_template": "Strong health ({health}) with solid trend ({trend}) and derivatives ({derivative})"
  },
  {
    "priority": 90,
    "signal": "WATCH",
    "logic_operator": "AND",
    "conditions": [
      {"field": "health",     "operator": ">=", "value": 75},
      {"field": "confidence", "operator": ">=", "value": 50}
    ],
    "reason_template": "Good health ({health}) with adequate confidence ({confidence})"
  },
  {
    "priority": 80,
    "signal": "WATCH",
    "logic_operator": "AND",
    "conditions": [
      {"field": "health",      "operator": ">=", "value": 65},
      {"field": "derivative",  "operator": ">=", "value": 85},
      {"field": "trend",       "operator": ">=", "value": 70}
    ],
    "reason_template": "Moderate health but very strong derivatives ({derivative}) suggest accumulation"
  },
  {
    "priority": 70,
    "signal": "OBSERVE",
    "logic_operator": "AND",
    "conditions": [
      {"field": "health", "operator": ">=", "value": 55},
      {"field": "health", "operator": "<",  "value": 75}
    ],
    "reason_template": "Moderate health ({health}), watching for trend direction"
  },
  {
    "priority": 60,
    "signal": "CAUTION",
    "logic_operator": "OR",
    "conditions": [
      {"field": "health",     "operator": "<",  "value": 50},
      {"field": "confidence", "operator": "<",  "value": 30}
    ],
    "reason_template": "Low health ({health}) or insufficient data confidence ({confidence})"
  },
  {
    "priority": 10,
    "signal": "WEAK",
    "logic_operator": "AND",
    "conditions": [
      {"field": "health", "operator": "<", "value": 40}
    ],
    "reason_template": "Weak health score ({health}), avoid or reduce exposure"
  }
]
```

---

## 5. MORNING SNAPSHOT NORMALIZATION (P1C)

### 5.1 Migration Strategy

```
Giữ bảng morning_snapshots cũ (backward compat)
Thêm 3 bảng mới normalized
Refresh pipeline writes to BOTH
Phase out cũ ở P2
```

### 5.2 Query Examples Sau Normalize

```sql
-- Top narrative 30 ngày gần nhất
SELECT
  n.name,
  AVG(msn.health_score) as avg_health,
  MAX(msn.health_score) as peak_health,
  MIN(msn.health_score) as trough_health,
  COUNT(*) as data_points
FROM morning_snapshot_narratives msn
JOIN morning_snapshot_headers msh ON msh.id = msn.snapshot_id
JOIN narratives n ON n.id = msn.narrative_id
WHERE msh.date >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY n.id, n.name
ORDER BY avg_health DESC;

-- Coin health trend 7 ngày
SELECT
  c.symbol,
  msc.health_score,
  msc.signal,
  msh.date
FROM morning_snapshot_coins msc
JOIN morning_snapshot_headers msh ON msh.id = msc.snapshot_id
JOIN coins c ON c.id = msc.coin_id
WHERE msc.coin_id = 1
  AND msh.date >= CURRENT_DATE - INTERVAL '7 days'
ORDER BY msh.date ASC;
```

---

## 6. INTERFACES P1

```typescript
// src/lib/types/indicator.ts

export interface IndicatorRecord {
  id:             number;
  coinId:         number;
  date:           string;
  timeframe:      string;
  indicatorType:  string;
  indicatorValue: number | null;
  indicatorMeta:  Record<string, unknown> | null;
  source:         string | null;
  calculatedAt:   Date;
}

export interface IndicatorBatch {
  coinId:    number;
  date:      string;
  timeframe: string;
  source:    string;
  values:    Array<{
    type:  string;
    value: number | null;
    meta?: Record<string, unknown>;
  }>;
}

// src/lib/types/recommendation-rule.ts

export interface RecommendationRule {
  id:             number;
  ruleVersionId:  number;
  priority:       number;
  signal:         string;
  logicOperator:  'AND' | 'OR';
  conditions:     RuleCondition[];
  reasonTemplate: string;
  isActive:       boolean;
  createdAt:      Date;
}

export interface RuleCondition {
  field:    'health' | 'trend' | 'derivative' | 'volume' | 'momentum' | 'confidence';
  operator: '>' | '>=' | '<' | '<=' | '==' | '!=';
  value:    number;
}

export interface ScoreInput {
  health:     number;
  trend:      number;
  derivative: number;
  volume:     number;
  momentum:   number;
  confidence: number;
}

export interface RecommendationResult {
  signal:    string;
  reason:    string;
  ruleId:    number | null;
  matched:   boolean;
}

// src/lib/types/morning-snapshot.ts

export interface MorningSnapshotHeader {
  id:             number;
  date:           string;
  totalCoins:     number;
  avgHealthScore: number;
  topNarrativeId: number | null;
  alertCount:     number;
  ruleVersionId:  number | null;
  timezone:       string;
  createdAt:      Date;
}

export interface MorningSnapshotCoin {
  id:          number;
  snapshotId:  number;
  coinId:      number;
  healthScore: number | null;
  scoreChange: number | null;
  signal:      string | null;
  confidence:  number | null;
}

export interface MorningSnapshotNarrative {
  id:             number;
  snapshotId:     number;
  narrativeId:    number;
  healthScore:    number | null;
  scoreChange:    number | null;
  coinCount:      number | null;
  topCoinId:      number | null;
  weakestCoinId:  number | null;
  weightingMethod:string | null;
}
```

---

## 7. API ENDPOINTS MỚI P1

```
GET  /api/indicators/[coinId]?date=&timeframe=
     → Lấy all indicators cho coin tại date/timeframe

GET  /api/indicators/[coinId]/history?days=30&type=RSI_14
     → Lịch sử 1 indicator type

GET  /api/admin/recommendation-rules
     → List all rules for active version

POST /api/admin/recommendation-rules
     → Create new rule

PUT  /api/admin/recommendation-rules/[id]
     → Update rule

DELETE /api/admin/recommendation-rules/[id]
     → Deactivate rule (soft delete)

GET  /api/snapshots?date=&limit=30
     → Morning snapshot history (normalized)

GET  /api/snapshots/[date]/narratives
     → Narrative breakdown for a specific snapshot

GET  /api/snapshots/[date]/coins
     → Coin breakdown for a specific snapshot
```

---

## 8. TESTING REQUIREMENTS P1

### Unit Tests

```
src/lib/indicators/__tests__/
  indicator-engine.test.ts      → P1A
  indicator-batch.test.ts       → P1A

src/lib/scoring/__tests__/
  rule-engine.test.ts           → P1B
  rule-evaluation.test.ts       → P1B

src/lib/services/__tests__/
  snapshot.service.test.ts      → P1C
```

### Critical Test Cases

```typescript
// P1B Rule Engine
describe('evaluateRules', () => {
  it('AND logic: all conditions must match')
  it('OR logic: any condition sufficient')
  it('Priority: higher priority rule wins when both match')
  it('Fallback: returns OBSERVE when no rule matches')
  it('Inactive rules: skipped in evaluation')
  it('Missing field: condition fails gracefully (not throw)')
  it('Strong Watch: health=87, trend=78, derivative=72 → STRONG_WATCH')
  it('Accumulation pattern: health=68, derivative=87, trend=71 → WATCH')
})

// P1A Indicator Engine
describe('calculateIndicators', () => {
  it('EMA_9 value matches manual calculation')
  it('RSI_14 value within acceptable range')
  it('Saves to indicators table with correct unique key')
  it('Upsert: duplicate date/timeframe/type updates not inserts')
  it('MACD meta contains signal and histogram')
})

// P1C Snapshot
describe('MorningSnapshotService', () => {
  it('Creates header record')
  it('Creates coin records for all active coins')
  it('Creates narrative records for all active narratives')
  it('Unique constraint prevents duplicate snapshots per date')
  it('Historical query returns correct time series')
})
```

---

## 9. DEPENDENCY MAP P1

```
P1E (analyzer.ts fix) → INDEPENDENT, do first
      ↓
P1A (Indicator Table)
  REQUIRES: DB migration
  BLOCKS: P1D (provenance), better feature calculation
      ↓
P1B (Rule Engine)
  REQUIRES: recommendation_rules table
  REQUIRES: rule_versions table (P0B, done ✅)
  BLOCKS: better recommendations
      ↓
P1C (Snapshot normalization)
  REQUIRES: morning_snapshot_headers/coins/narratives tables
  INDEPENDENT of P1A and P1B (can parallel)
      ↓
P1D (Feature Provenance)
  REQUIRES: P1A (indicators table)
  REQUIRES: features table alter
```

RECOMMENDED ORDER:
```
Day 1: P1E (30 min) + P1A migration
Day 2: P1A service + P1B migration
Day 3: P1B Rule Engine + P1C migration
Day 4: P1C service + P1D
Day 5: APIs + UI updates
Day 6: Testing + Review
```
```

---

# 📄 02_P2_Implementation_Pack.md

```markdown
# P2 Implementation Pack
# Sprint: Decision Engine + Advanced Analytics
# Version: 1.0
# Date: TBD (after P1 complete)
# Prerequisites: P0 ✅, P1 ✅

---

## 1. SPRINT OVERVIEW

### 1.1 Mục Tiêu P2

Sau P0 + P1, hệ thống có đủ foundation để build:
- Indicator layer ✅
- Rule Engine ✅
- Version tracking ✅

P2 adds Intelligence layer:

| ID  | Feature                              | Impact   |
|-----|--------------------------------------|----------|
| P2A | Decision Engine (Risk-adjusted)      | CRITICAL |
| P2B | Event Risk Framework                 | HIGH     |
| P2C | Correlation Analysis                 | MEDIUM   |
| P2D | Narrative Momentum Score             | MEDIUM   |
| P2E | Advanced Admin Analytics             | MEDIUM   |
| P2F | Alert System (thresholds + triggers) | HIGH     |

### 1.2 Định Nghĩa Done

- [ ] Decision Engine overlay trên Health Score
- [ ] Event Risk records có thể input thủ công
- [ ] Correlation matrix giữa coins trong narrative
- [ ] Narrative Momentum (trend of trends)
- [ ] Admin có charts phân tích hiệu quả rules
- [ ] Alert triggers và history

---

## 2. KIẾN TRÚC P2

### 2.1 Full Stack Sau P2

```
┌─────────────────────────────────────────────────────────┐
│                    DATA LAYER                           │
│  Binance Spot/Futures  CoinGecko  Manual Events        │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│                  INDICATOR LAYER (P1A)                  │
│  EMA RSI MACD ADX BB ATR VWAP OI Funding Liquidation   │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│                  FEATURE LAYER                          │
│  Trend  Derivative  Volume  Momentum  (weighted)        │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│               HEALTH SCORE LAYER                        │
│  Weighted composite  Confidence  Version tracked        │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│           DECISION ENGINE LAYER (P2A) ← NEW             │
│                                                         │
│  Health Score                                           │
│       +                                                 │
│  Event Risk Score  ← Event Risk Framework (P2B)         │
│       +                                                 │
│  Correlation Risk  ← Correlation Analysis (P2C)         │
│       ↓                                                 │
│  Adjusted Signal                                        │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│              RECOMMENDATION LAYER (P1B)                 │
│  Multi-condition Rule Engine                            │
│  NOW receives: health + event_risk + correlation        │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│                  ALERT LAYER (P2F)                      │
│  Threshold triggers  Score drops  Signal changes        │
└─────────────────────────────────────────────────────────┘
```

### 2.2 Database Changes P2

```sql
-- Event Risk
CREATE TABLE event_risks (
  id              SERIAL PRIMARY KEY,
  coin_id         INTEGER REFERENCES coins(id),
  narrative_id    INTEGER REFERENCES narratives(id),
  event_type      VARCHAR(30) NOT NULL,
  event_date      DATE NOT NULL,
  risk_level      VARCHAR(10) NOT NULL,  -- 'LOW','MEDIUM','HIGH','CRITICAL'
  risk_score      DECIMAL(5,2),          -- 0-100 (100 = maximum risk)
  title           VARCHAR(200) NOT NULL,
  description     TEXT,
  source_url      TEXT,
  is_active       BOOLEAN DEFAULT TRUE,
  created_at      TIMESTAMP DEFAULT NOW(),
  expires_at      DATE                   -- when risk expires
);

-- Correlation Matrix (computed daily)
CREATE TABLE coin_correlations (
  id              SERIAL PRIMARY KEY,
  date            DATE NOT NULL,
  coin_id_a       INTEGER NOT NULL REFERENCES coins(id),
  coin_id_b       INTEGER NOT NULL REFERENCES coins(id),
  correlation     DECIMAL(5,4),          -- -1 to +1
  period_days     INTEGER DEFAULT 30,
  created_at      TIMESTAMP DEFAULT NOW(),
  UNIQUE(date, coin_id_a, coin_id_b, period_days)
);

-- Narrative Momentum
CREATE TABLE narrative_momentum (
  id              SERIAL PRIMARY KEY,
  narrative_id    INTEGER NOT NULL REFERENCES narratives(id),
  date            DATE NOT NULL,
  momentum_score  DECIMAL(5,2),          -- rate of change of health score
  momentum_type   VARCHAR(20),           -- 'accelerating','decelerating','stable'
  health_7d_ago   DECIMAL(5,2),
  health_now      DECIMAL(5,2),
  created_at      TIMESTAMP DEFAULT NOW(),
  UNIQUE(narrative_id, date)
);

-- Decision Engine Output
CREATE TABLE decision_signals (
  id              SERIAL PRIMARY KEY,
  coin_id         INTEGER NOT NULL REFERENCES coins(id),
  date            DATE NOT NULL,
  base_health     DECIMAL(5,2),          -- raw health score
  event_risk_score DECIMAL(5,2),         -- 0-100 (higher = more risky)
  adjusted_score  DECIMAL(5,2),          -- health after risk adjustment
  adjustment_reason TEXT,
  active_events   JSONB,                 -- snapshot of active event risks
  created_at      TIMESTAMP DEFAULT NOW(),
  UNIQUE(coin_id, date)
);

-- Alert Rules
CREATE TABLE alert_rules (
  id              SERIAL PRIMARY KEY,
  name            VARCHAR(100) NOT NULL,
  scope           VARCHAR(10) NOT NULL,   -- 'coin', 'narrative', 'global'
  scope_id        INTEGER,               -- coin_id or narrative_id (null=global)
  trigger_type    VARCHAR(30) NOT NULL,   -- 'score_below', 'score_drop', 'signal_change'
  trigger_value   DECIMAL(10,2),
  is_active       BOOLEAN DEFAULT TRUE,
  created_at      TIMESTAMP DEFAULT NOW()
);

-- Alert History
CREATE TABLE alert_history (
  id              SERIAL PRIMARY KEY,
  rule_id         INTEGER NOT NULL REFERENCES alert_rules(id),
  triggered_at    TIMESTAMP NOT NULL DEFAULT NOW(),
  trigger_detail  JSONB,
  acknowledged_at TIMESTAMP,
  acknowledged_by VARCHAR(100)
);
```

---

## 3. DECISION ENGINE (P2A)

### 3.1 Concept

```
Decision Engine không thay thế Health Score.
Nó là một OVERLAY điều chỉnh signal.

Health Score = "Coin đang khỏe thế nào?"
Decision Signal = "Hôm nay tôi có nên trading không?"

Ví dụ:
  CARV: Health = 86 (HEALTHY)
  CARV Event Risk: Unlock tomorrow (CRITICAL)
  Decision: CAUTION (downgrade signal)

  AI16Z: Health = 72 (NEUTRAL)
  AI16Z Event Risk: None
  AI16Z Correlation: Low (diversified)
  Decision: WATCH (upgrade nếu derivatives strong)
```

### 3.2 Adjustment Formula

```typescript
interface DecisionInput {
  coinId:         number;
  date:           string;
  healthScore:    number;       // 0-100
  eventRiskScore: number;       // 0-100 (100=max risk)
  correlationRisk: number;      // 0-100 (100=highly correlated narrative)
}

function calculateAdjustedScore(input: DecisionInput): {
  adjustedScore:    number;
  adjustmentReason: string;
} {
  let score  = input.healthScore;
  let reason = '';

  // Event Risk Penalty
  if (input.eventRiskScore >= 80) {
    const penalty = 25;
    score -= penalty;
    reason += `Critical event risk (-${penalty}pts). `;
  } else if (input.eventRiskScore >= 60) {
    const penalty = 15;
    score -= penalty;
    reason += `High event risk (-${penalty}pts). `;
  } else if (input.eventRiskScore >= 40) {
    const penalty = 8;
    score -= penalty;
    reason += `Moderate event risk (-${penalty}pts). `;
  }

  // Correlation Risk (narrative concentration)
  if (input.correlationRisk >= 80) {
    const penalty = 10;
    score -= penalty;
    reason += `High narrative correlation (-${penalty}pts). `;
  }

  // Floor at 5 (never completely zero unless truly broken)
  score = Math.max(5, Math.min(100, score));

  return {
    adjustedScore:    Math.round(score * 100) / 100,
    adjustmentReason: reason.trim() || 'No risk adjustments applied',
  };
}
```

### 3.3 Event Risk Types

```typescript
export const EVENT_RISK_TYPES = {
  TOKEN_UNLOCK:      { defaultRiskLevel: 'HIGH',     defaultScore: 70 },
  PROTOCOL_UPGRADE:  { defaultRiskLevel: 'MEDIUM',   defaultScore: 40 },
  REGULATORY_NEWS:   { defaultRiskLevel: 'HIGH',     defaultScore: 65 },
  HACK_EXPLOIT:      { defaultRiskLevel: 'CRITICAL', defaultScore: 90 },
  TEAM_CHANGE:       { defaultRiskLevel: 'MEDIUM',   defaultScore: 45 },
  PARTNERSHIP:       { defaultRiskLevel: 'LOW',      defaultScore: 10 },  // Positive event
  LISTING:           { defaultRiskLevel: 'LOW',      defaultScore: 15 },  // Positive
  VESTING_END:       { defaultRiskLevel: 'HIGH',     defaultScore: 75 },
  AUDIT_ISSUE:       { defaultRiskLevel: 'CRITICAL', defaultScore: 85 },
  LIQUIDITY_CRISIS:  { defaultRiskLevel: 'CRITICAL', defaultScore: 95 },
} as const;
```

---

## 4. EVENT RISK FRAMEWORK (P2B)

### 4.1 Manual Input UI

```
Admin Panel → Events Tab

Form fields:
  - Coin or Narrative (select)
  - Event Type (dropdown from EVENT_RISK_TYPES)
  - Event Date
  - Risk Level override (auto-filled, can change)
  - Risk Score override (0-100)
  - Title
  - Description
  - Source URL
  - Expires At (date when risk no longer applies)
```

### 4.2 Event Risk Score Calculation

```typescript
function getCoinEventRiskScore(
  coinId: number,
  date: string,
  events: EventRisk[]
): number {

  // Filter active events for this coin on this date
  const activeEvents = events.filter(e =>
    e.coinId === coinId &&
    e.isActive &&
    e.eventDate <= date &&
    (!e.expiresAt || e.expiresAt >= date)
  );

  if (activeEvents.length === 0) return 0;

  // Max risk from any single event (not sum, to avoid inflation)
  const maxRisk = Math.max(...activeEvents.map(e => e.riskScore ?? 0));

  // Small bonus for multiple events (max +15)
  const multiEventBonus = Math.min((activeEvents.length - 1) * 5, 15);

  return Math.min(maxRisk + multiEventBonus, 100);
}
```

---

## 5. CORRELATION ANALYSIS (P2C)

### 5.1 Correlation Calculation

```typescript
function calculatePearsonCorrelation(
  series1: number[],  // daily health scores coin A
  series2: number[]   // daily health scores coin B
): number {
  // Standard Pearson correlation
  const n = Math.min(series1.length, series2.length);
  if (n < 5) return 0;  // Not enough data

  const mean1 = series1.slice(-n).reduce((a, b) => a + b) / n;
  const mean2 = series2.slice(-n).reduce((a, b) => a + b) / n;

  let num = 0;
  let den1 = 0;
  let den2 = 0;

  for (let i = 0; i < n; i++) {
    const d1 = series1[i] - mean1;
    const d2 = series2[i] - mean2;
    num  += d1 * d2;
    den1 += d1 * d1;
    den2 += d2 * d2;
  }

  const denom = Math.sqrt(den1 * den2);
  return denom === 0 ? 0 : num / denom;
}
```

### 5.2 Narrative Correlation Risk

```typescript
function getNarrativeCorrelationRisk(
  correlations: number[]   // all pairwise correlations within narrative
): number {
  if (correlations.length === 0) return 0;
  const avgCorrelation = correlations.reduce((a, b) => a + b) / correlations.length;
  // avgCorr 0 → risk 0, avgCorr 1 → risk 100
  return Math.round(Math.max(0, avgCorrelation) * 100);
}
```

---

## 6. NARRATIVE MOMENTUM (P2D)

### 6.1 Concept

```
Narrative Health Timeline:
  Day 1:  72
  Day 2:  74
  Day 3:  78
  Day 4:  81   ← Momentum ACCELERATING (slope increasing)
  Day 5:  83
  Day 6:  84
  Day 7:  84.5 ← Momentum DECELERATING (slope decreasing)

Momentum Score = rate of change of health score
  Positive → Narrative improving
  Negative → Narrative deteriorating
  Near zero → Stable or topping
```

### 6.2 Momentum Calculation

```typescript
function calculateNarrativeMomentum(
  healthHistory: Array<{ date: string; healthScore: number }>
): NarrativeMomentum {

  if (healthHistory.length < 3) {
    return { score: 0, type: 'stable', health7dAgo: null, healthNow: null };
  }

  const recent = healthHistory.slice(-7);
  const now    = recent[recent.length - 1].healthScore;
  const ago7d  = recent[0].healthScore;

  // First derivative (rate of change)
  const change7d = now - ago7d;

  // Second derivative (rate of rate of change)
  // Compare first half vs second half slope
  const midpoint = Math.floor(recent.length / 2);
  const firstHalfSlope  = linearSlope(recent.slice(0, midpoint));
  const secondHalfSlope = linearSlope(recent.slice(midpoint));
  const acceleration    = secondHalfSlope - firstHalfSlope;

  let type: 'accelerating' | 'decelerating' | 'stable';
  if      (Math.abs(acceleration) < 0.5) type = 'stable';
  else if (acceleration > 0)              type = 'accelerating';
  else                                    type = 'decelerating';

  // Momentum score: -100 to +100
  const momentumScore = Math.round(Math.max(-100, Math.min(100, change7d * 10)));

  return {
    score:       momentumScore,
    type,
    health7dAgo: ago7d,
    healthNow:   now,
  };
}
```

---

## 7. ALERT SYSTEM (P2F)

### 7.1 Alert Trigger Types

```typescript
export const TRIGGER_TYPES = {
  SCORE_BELOW: {
    description: 'Health score drops below threshold',
    evaluate: (current: number, threshold: number) => current < threshold,
  },
  SCORE_DROP: {
    description: 'Health score drops by X points vs yesterday',
    evaluate: (change: number, threshold: number) => change < -threshold,
  },
  SCORE_RISE: {
    description: 'Health score rises by X points vs yesterday',
    evaluate: (change: number, threshold: number) => change > threshold,
  },
  SIGNAL_CHANGE: {
    description: 'Recommendation signal changes',
    evaluate: (prev: string, curr: string) => prev !== curr,
  },
  NARRATIVE_WEAK: {
    description: 'Narrative health drops below threshold',
    evaluate: (current: number, threshold: number) => current < threshold,
  },
} as const;
```

### 7.2 Alert Evaluation

```typescript
async function evaluateAlerts(
  date: string,
  coinScores: CoinScoreMap,
  previousScores: CoinScoreMap
): Promise<AlertFired[]> {

  const activeRules = await getActiveAlertRules();
  const fired: AlertFired[] = [];

  for (const rule of activeRules) {
    const current  = coinScores[rule.scopeId ?? 'global'];
    const previous = previousScores[rule.scopeId ?? 'global'];
    if (!current) continue;

    let triggered = false;
    switch (rule.triggerType) {
      case 'SCORE_BELOW':
        triggered = current.healthScore < rule.triggerValue;
        break;
      case 'SCORE_DROP':
        if (previous) {
          triggered = (current.healthScore - previous.healthScore) < -rule.triggerValue;
        }
        break;
      case 'SIGNAL_CHANGE':
        if (previous) {
          triggered = current.signal !== previous.signal;
        }
        break;
    }

    if (triggered) {
      fired.push({
        ruleId: rule.id,
        triggerDetail: { current, previous, threshold: rule.triggerValue },
      });
    }
  }

  return fired;
}
```

---

## 8. API ENDPOINTS P2

```
# Decision Engine
GET  /api/coins/[id]/decision?date=
     → adjusted score + event risk + correlation risk

# Events
GET  /api/events?coinId=&narrativeId=&active=true
     → list events

POST /api/admin/events
     → create event risk record

PUT  /api/admin/events/[id]
     → update event

DELETE /api/admin/events/[id]
     → deactivate event

# Correlations
GET  /api/narratives/[id]/correlations?days=30
     → correlation matrix for all coins in narrative

# Momentum
GET  /api/narratives/[id]/momentum?days=30
     → narrative momentum timeline

# Alerts
GET  /api/admin/alerts/rules
     → list alert rules

POST /api/admin/alerts/rules
     → create alert rule

GET  /api/admin/alerts/history?limit=50
     → recent alert history

POST /api/admin/alerts/[id]/acknowledge
     → mark alert as acknowledged

# Analytics
GET  /api/admin/analytics/rule-effectiveness
     → how often each rule fires, accuracy vs market movement

GET  /api/admin/analytics/narrative-performance?days=30
     → narrative health over time comparison
```

---

## 9. UI COMPONENTS P2

### 9.1 Decision Signal Card

```
┌─────────────────────────────────────┐
│  CARV                               │
│  Health: 86 (HEALTHY)               │
│                                     │
│  ⚠️  Event Risk: HIGH               │
│  Token Unlock in 18 hours           │
│                                     │
│  Adjusted Score: 61 (CAUTION)  ↓    │
│  Recommendation: CAUTION            │
│  "High event risk (-25pts). Token   │
│   unlock creates sell pressure."    │
└─────────────────────────────────────┘
```

### 9.2 Correlation Matrix Heatmap

```
Narrative: AI Agents
         VIRTUAL  AI16Z  AIXBT  AGENT
VIRTUAL  [1.00]  [0.72]  [0.45]  [0.61]
AI16Z   [0.72]  [1.00]  [0.38]  [0.55]
AIXBT   [0.45]  [0.38]  [1.00]  [0.29]
AGENT   [0.61]  [0.55]  [0.29]  [1.00]

Color: Red(high) → Yellow(mid) → Green(low)
Avg Correlation: 0.50 → Moderate concentration risk
```

### 9.3 Event Timeline

```
Event Risk Timeline for CARV:
  
  Aug 03 ────────────────────── Aug 10
         │
         ├── Aug 05: Protocol Upgrade (MEDIUM)
         │          Risk: 40pts | Expires: Aug 07
         │
         └── Aug 09: Token Unlock (HIGH)
                    Risk: 70pts | Expires: Aug 10
```

### 9.4 Alert Bell

```
Dashboard header:
  [🔔 3]  ← 3 unacknowledged alerts

Alert dropdown:
  ⚠️ CARV score dropped 15pts (now 71) - 2h ago
  ⚠️ AI16Z signal changed WATCH→OBSERVE - 5h ago
  ✅ VIRTUAL score crossed 90 (STRONG) - 1d ago [ACK]
```

---

## 10. DEPENDENCY MAP P2

```
P0 (Complete ✅)
P1 (Complete required)
      ↓
P2A Decision Engine
  REQUIRES: P1B Rule Engine (for multi-condition evaluation)
  REQUIRES: event_risks table (P2B)
  REQUIRES: coin_correlations table (P2C)
  BLOCKS: Full recommendation pipeline

P2B Event Risk Framework
  REQUIRES: event_risks table
  BLOCKS: P2A (input to decision engine)
  PARALLEL with P2C

P2C Correlation Analysis
  REQUIRES: health_scores history (30+ days)
  REQUIRES: coin_correlations table
  BLOCKS: P2A (input to decision engine)
  PARALLEL with P2B

P2D Narrative Momentum
  REQUIRES: narrative_health history (P0A, done ✅)
  REQUIRES: narrative_momentum table
  INDEPENDENT of P2A, B, C

P2E Admin Analytics
  REQUIRES: P1B (rule tracking)
  REQUIRES: decision_signals table (P2A)
  INDEPENDENT

P2F Alert System
  REQUIRES: alert_rules, alert_history tables
  REQUIRES: refresh pipeline (trigger point)
  PARALLEL with P2A-E
  BLOCKS: Nothing (enhancement only)

RECOMMENDED ORDER:
  Week 1: P2B + P2C (foundation for P2A)
  Week 2: P2A Decision Engine
  Week 3: P2D + P2F (independent features)
  Week 4: P2E Analytics + UI
  Week 5: Integration testing + polish
```

---

## 11. ACCEPTANCE CRITERIA P2

### P2A Decision Engine
- [ ] Adjusted score differs from health score when events active
- [ ] CRITICAL event causes ≥20pt penalty
- [ ] No event → adjusted score = health score
- [ ] Adjustment reason stored in decision_signals

### P2B Event Risk
- [ ] Admin can create/edit/deactivate events
- [ ] Events expire automatically after expires_at date
- [ ] Risk score properly flows into Decision Engine
- [ ] Multi-event risk: max + small bonus (not sum)

### P2C Correlation Analysis
- [ ] Pearson correlation calculated daily after refresh
- [ ] Correlation matrix displays in Admin/Narrative view
- [ ] High correlation narrative shows warning badge

### P2D Narrative Momentum
- [ ] Momentum score calculated after each refresh
- [ ] Momentum chart visible on Narrative detail
- [ ] Type (accelerating/decelerating/stable) displayed

### P2E Admin Analytics
- [ ] Rule effectiveness shows fire rate per rule
- [ ] Narrative performance comparison chart

### P2F Alert System
- [ ] Score drop alert triggers correctly
- [ ] Alert history queryable
- [ ] Acknowledge button works
- [ ] Alert count badge on dashboard header
```

---

# 📁 02_Agent_Work_Pack_P1/

## Agent A-P1: `A_P1_Database.md`

```markdown
# Agent A-P1: Database Migration P1
# Depends on: P0 complete

═══════════════════════════════════════════════════════════
TASK 1: indicators table
═══════════════════════════════════════════════════════════

File: drizzle/migrations/0005_add_indicators.sql

CREATE TABLE IF NOT EXISTS indicators (
  id              SERIAL PRIMARY KEY,
  coin_id         INTEGER NOT NULL REFERENCES coins(id) ON DELETE CASCADE,
  date            DATE NOT NULL,
  timeframe       VARCHAR(10) NOT NULL,
  indicator_type  VARCHAR(50) NOT NULL,
  indicator_value DECIMAL(20,8),
  indicator_meta  JSONB,
  source          VARCHAR(30),
  calculated_at   TIMESTAMP NOT NULL DEFAULT NOW(),
  CONSTRAINT indicators_unique
    UNIQUE(coin_id, date, timeframe, indicator_type)
);

CREATE INDEX IF NOT EXISTS idx_indicators_coin_date
  ON indicators(coin_id, date);
CREATE INDEX IF NOT EXISTS idx_indicators_type
  ON indicators(coin_id, indicator_type, date DESC);

═══════════════════════════════════════════════════════════
TASK 2: recommendation_rules table
═══════════════════════════════════════════════════════════

File: drizzle/migrations/0006_add_recommendation_rules.sql

CREATE TABLE IF NOT EXISTS recommendation_rules (
  id               SERIAL PRIMARY KEY,
  rule_version_id  INTEGER NOT NULL REFERENCES rule_versions(id),
  priority         INTEGER NOT NULL DEFAULT 50,
  signal           VARCHAR(20) NOT NULL,
  logic_operator   VARCHAR(5) NOT NULL DEFAULT 'AND'
    CHECK (logic_operator IN ('AND', 'OR')),
  conditions       JSONB NOT NULL DEFAULT '[]',
  reason_template  TEXT NOT NULL DEFAULT '',
  is_active        BOOLEAN NOT NULL DEFAULT TRUE,
  created_at       TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_rec_rules_version_active
  ON recommendation_rules(rule_version_id, is_active);
CREATE INDEX IF NOT EXISTS idx_rec_rules_priority
  ON recommendation_rules(priority DESC);

-- Seed default rules cho version 1
-- (Insert JSON từ spec section 4.3)
INSERT INTO recommendation_rules
  (rule_version_id, priority, signal, logic_operator, conditions, reason_template)
SELECT
  rv.id, r.priority, r.signal, r.logic_operator,
  r.conditions::jsonb, r.reason_template
FROM rule_versions rv,
(VALUES
  (100, 'STRONG_WATCH', 'AND',
   '[{"field":"health","operator":">=","value":85},{"field":"trend","operator":">=","value":75},{"field":"derivative","operator":">=","value":70},{"field":"confidence","operator":">=","value":60}]',
   'Strong health ({health}) with solid trend ({trend}) and derivatives ({derivative})'
  ),
  (90, 'WATCH', 'AND',
   '[{"field":"health","operator":">=","value":75},{"field":"confidence","operator":">=","value":50}]',
   'Good health ({health}) with adequate confidence ({confidence})'
  ),
  (80, 'WATCH', 'AND',
   '[{"field":"health","operator":">=","value":65},{"field":"derivative","operator":">=","value":85},{"field":"trend","operator":">=","value":70}]',
   'Moderate health but strong derivatives ({derivative}) suggest accumulation'
  ),
  (70, 'OBSERVE', 'AND',
   '[{"field":"health","operator":">=","value":55},{"field":"health","operator":"<","value":75}]',
   'Moderate health ({health}), watching for trend direction'
  ),
  (60, 'CAUTION', 'OR',
   '[{"field":"health","operator":"<","value":50},{"field":"confidence","operator":"<","value":30}]',
   'Low health ({health}) or insufficient data confidence ({confidence})'
  ),
  (10, 'WEAK', 'AND',
   '[{"field":"health","operator":"<","value":40}]',
   'Weak health score ({health}), avoid or reduce exposure'
  )
) AS r(priority, signal, logic_operator, conditions, reason_template)
WHERE rv.version = 1
ON CONFLICT DO NOTHING;

═══════════════════════════════════════════════════════════
TASK 3: Morning Snapshot Normalized Tables
═══════════════════════════════════════════════════════════

File: drizzle/migrations/0007_add_snapshot_normalized.sql

CREATE TABLE IF NOT EXISTS morning_snapshot_headers (
  id               SERIAL PRIMARY KEY,
  date             DATE NOT NULL UNIQUE,
  total_coins      INTEGER,
  avg_health_score DECIMAL(5,2),
  top_narrative_id INTEGER REFERENCES narratives(id),
  alert_count      INTEGER DEFAULT 0,
  rule_version_id  INTEGER REFERENCES rule_versions(id),
  timezone         VARCHAR(50) DEFAULT 'Asia/Ho_Chi_Minh',
  created_at       TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS morning_snapshot_coins (
  id           SERIAL PRIMARY KEY,
  snapshot_id  INTEGER NOT NULL
    REFERENCES morning_snapshot_headers(id) ON DELETE CASCADE,
  coin_id      INTEGER NOT NULL REFERENCES coins(id),
  health_score DECIMAL(5,2),
  score_change DECIMAL(5,2),
  signal       VARCHAR(20),
  confidence   DECIMAL(5,2),
  UNIQUE(snapshot_id, coin_id)
);

CREATE TABLE IF NOT EXISTS morning_snapshot_narratives (
  id              SERIAL PRIMARY KEY,
  snapshot_id     INTEGER NOT NULL
    REFERENCES morning_snapshot_headers(id) ON DELETE CASCADE,
  narrative_id    INTEGER NOT NULL REFERENCES narratives(id),
  health_score    DECIMAL(5,2),
  score_change    DECIMAL(5,2),
  coin_count      INTEGER,
  top_coin_id     INTEGER REFERENCES coins(id),
  weakest_coin_id INTEGER REFERENCES coins(id),
  weighting_method VARCHAR(20),
  UNIQUE(snapshot_id, narrative_id)
);

CREATE INDEX IF NOT EXISTS idx_snap_coins_snapshot
  ON morning_snapshot_coins(snapshot_id);
CREATE INDEX IF NOT EXISTS idx_snap_narratives_snapshot
  ON morning_snapshot_narratives(snapshot_id);

═══════════════════════════════════════════════════════════
TASK 4: Feature Provenance
═══════════════════════════════════════════════════════════

File: drizzle/migrations/0008_alter_features_provenance.sql

ALTER TABLE features
  ADD COLUMN IF NOT EXISTS source_provenance JSONB,
  ADD COLUMN IF NOT EXISTS calculated_at     TIMESTAMP;

UPDATE features
  SET calculated_at = created_at
  WHERE calculated_at IS NULL;

═══════════════════════════════════════════════════════════
TASK 5: Drizzle Schema Updates
═══════════════════════════════════════════════════════════

Thêm vào src/lib/db/schema.ts:

export const indicators = pgTable('indicators', {
  id:             serial('id').primaryKey(),
  coinId:         integer('coin_id').notNull().references(() => coins.id),
  date:           date('date').notNull(),
  timeframe:      varchar('timeframe', { length: 10 }).notNull(),
  indicatorType:  varchar('indicator_type', { length: 50 }).notNull(),
  indicatorValue: decimal('indicator_value', { precision: 20, scale: 8 }),
  indicatorMeta:  jsonb('indicator_meta'),
  source:         varchar('source', { length: 30 }),
  calculatedAt:   timestamp('calculated_at').notNull().defaultNow(),
});

export const recommendationRules = pgTable('recommendation_rules', {
  id:             serial('id').primaryKey(),
  ruleVersionId:  integer('rule_version_id').notNull()
    .references(() => ruleVersions.id),
  priority:       integer('priority').notNull().default(50),
  signal:         varchar('signal', { length: 20 }).notNull(),
  logicOperator:  varchar('logic_operator', { length: 5 })
    .notNull().default('AND'),
  conditions:     jsonb('conditions').notNull(),
  reasonTemplate: text('reason_template').notNull(),
  isActive:       boolean('is_active').notNull().default(true),
  createdAt:      timestamp('created_at').defaultNow(),
});

export const morningSnapshotHeaders = pgTable('morning_snapshot_headers', {
  id:             serial('id').primaryKey(),
  date:           date('date').notNull().unique(),
  totalCoins:     integer('total_coins'),
  avgHealthScore: decimal('avg_health_score', { precision: 5, scale: 2 }),
  topNarrativeId: integer('top_narrative_id').references(() => narratives.id),
  alertCount:     integer('alert_count').default(0),
  ruleVersionId:  integer('rule_version_id').references(() => ruleVersions.id),
  timezone:       varchar('timezone', { length: 50 })
    .default('Asia/Ho_Chi_Minh'),
  createdAt:      timestamp('created_at').defaultNow(),
});

export const morningSnapshotCoins = pgTable('morning_snapshot_coins', {
  id:          serial('id').primaryKey(),
  snapshotId:  integer('snapshot_id').notNull()
    .references(() => morningSnapshotHeaders.id),
  coinId:      integer('coin_id').notNull().references(() => coins.id),
  healthScore: decimal('health_score', { precision: 5, scale: 2 }),
  scoreChange: decimal('score_change', { precision: 5, scale: 2 }),
  signal:      varchar('signal', { length: 20 }),
  confidence:  decimal('confidence', { precision: 5, scale: 2 }),
});

export const morningSnapshotNarratives = pgTable('morning_snapshot_narratives', {
  id:             serial('id').primaryKey(),
  snapshotId:     integer('snapshot_id').notNull()
    .references(() => morningSnapshotHeaders.id),
  narrativeId:    integer('narrative_id').notNull()
    .references(() => narratives.id),
  healthScore:    decimal('health_score', { precision: 5, scale: 2 }),
  scoreChange:    decimal('score_change', { precision: 5, scale: 2 }),
  coinCount:      integer('coin_count'),
  topCoinId:      integer('top_coin_id').references(() => coins.id),
  weakestCoinId:  integer('weakest_coin_id').references(() => coins.id),
  weightingMethod: varchar('weighting_method', { length: 20 }),
});

═══════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════

□ 0005_add_indicators.sql
□ 0006_add_recommendation_rules.sql
□ 0007_add_snapshot_normalized.sql
□ 0008_alter_features_provenance.sql
□ Drizzle schema updated (all 4 new tables)
□ npx drizzle-kit push → success
□ Default rules seeded (6 rules for version 1)
□ Rollback script for P1 migrations
□ Report: "A-P1-DONE"
```

---

## Agent B-P1: `B_P1_Services.md`

```markdown
# Agent B-P1: Service Layer P1
# Depends on: A-P1-DONE

═══════════════════════════════════════════════════════════
TASK 1: Indicator Engine Service
═══════════════════════════════════════════════════════════

File MỚI: src/lib/services/indicator.service.ts

CLASS: IndicatorService

METHOD: calculateAndSave(
  data: KlineData[],
  coinId: number,
  date: string,
  timeframe: string,
  source: string
): Promise<void>

  Calculate indicators từ KlineData:
    EMA_9:   ema(closes, 9).at(-1)
    EMA_21:  ema(closes, 21).at(-1)
    EMA_50:  ema(closes, 50).at(-1)
    EMA_200: ema(closes, 200).at(-1)
    RSI_14:  rsi(closes, 14).at(-1)
    ADX_14:  adxFull(data, 14).adx.at(-1)
    MACD:    value=macdLine.at(-1), meta={signal, histogram}
    BB_20:   value=middle.at(-1), meta={upper, lower, pctB}
    ATR_14:  atr(data, 14).at(-1)
    VWAP_20: vwapRollingAnalysis(...).value

  UPSERT vào indicators table (ON CONFLICT UPDATE)
  Use INSERT ... ON CONFLICT (coin_id, date, timeframe, indicator_type)
  DO UPDATE SET indicator_value=..., calculated_at=NOW()

METHOD: getIndicators(
  coinId: number,
  date: string,
  timeframe?: string
): Promise<IndicatorRecord[]>

METHOD: getIndicatorHistory(
  coinId: number,
  indicatorType: string,
  days: number
): Promise<Array<{ date: string; value: number | null }>>

Export singleton: export const indicatorService = new IndicatorService();

═══════════════════════════════════════════════════════════
TASK 2: Rule Engine Service
═══════════════════════════════════════════════════════════

File MỚI: src/lib/services/rule-engine.service.ts

CLASS: RuleEngineService

METHOD: getRulesForVersion(versionId: number): Promise<RecommendationRule[]>
  - Query recommendation_rules WHERE rule_version_id=? AND is_active=true
  - ORDER BY priority DESC

METHOD: evaluate(
  scores: ScoreInput,
  versionId: number
): Promise<RecommendationResult>

  1. Get rules for version
  2. For each rule (sorted by priority DESC):
     a. Skip if !isActive
     b. Evaluate all conditions based on logicOperator
     c. If matches: format reason, return result
  3. If no match: return fallback OBSERVE

METHOD: formatReason(template: string, scores: ScoreInput): string
  Replace {health} with scores.health.toFixed(1)
  Replace {trend} with scores.trend.toFixed(1)
  etc.

METHOD: createRule(
  input: CreateRuleInput,
  ruleVersionId: number
): Promise<RecommendationRule>
  - Validate: priority > 0
  - Validate: conditions array not empty
  - Validate: signal is valid value
  - Insert and return

METHOD: updateRule(id: number, updates: Partial<CreateRuleInput>): Promise<RecommendationRule>
METHOD: deactivateRule(id: number): Promise<void>

CRITICAL BEHAVIOR:
  - evaluateCondition(scores, condition) phải return false nếu field không tồn tại
  - Không throw khi field missing, chỉ return false
  - AND: tất cả conditions phải true
  - OR: ít nhất 1 condition phải true
  - Nếu rules = [] → fallback OBSERVE

Export singleton: export const ruleEngineService = new RuleEngineService();

═══════════════════════════════════════════════════════════
TASK 3: Morning Snapshot Service (Normalized)
═══════════════════════════════════════════════════════════

File MỚI: src/lib/services/snapshot.service.ts

CLASS: SnapshotService

METHOD: createDailySnapshot(
  date: string,
  coinScores: CoinScoreData[],
  narrativeScores: NarrativeScoreData[],
  ruleVersionId: number
): Promise<number>   // returns snapshot header ID

  1. Check existing: if header for date exists → return existing id
  2. Insert morning_snapshot_headers
  3. Insert morning_snapshot_coins (one row per coin)
  4. Insert morning_snapshot_narratives (one row per narrative)
  5. Return header id

METHOD: getSnapshotByDate(date: string): Promise<FullSnapshot | null>
  - Join header + coins + narratives
  - Return combined object

METHOD: getSnapshotHistory(days: number): Promise<SnapshotSummary[]>
  - Return header records with top narrative name

METHOD: getNarrativeHistory(
  narrativeId: number,
  days: number
): Promise<NarrativeSnapshotPoint[]>
  - Join morning_snapshot_narratives + morning_snapshot_headers
  - WHERE narrative_id=? AND date >= X
  - ORDER BY date ASC

METHOD: getCoinHistory(
  coinId: number,
  days: number
): Promise<CoinSnapshotPoint[]>
  - Similar but for coins

Export singleton: export const snapshotService = new SnapshotService();

═══════════════════════════════════════════════════════════
TASK 4: Update Refresh Pipeline
═══════════════════════════════════════════════════════════

Tìm refresh pipeline, THÊM:

1. After collecting kline data for each coin:
   await indicatorService.calculateAndSave(klines, coinId, date, '1d', source);
   await indicatorService.calculateAndSave(klines4h, coinId, date, '4h', source);

2. Replace old recommendation logic với rule engine:
   const recommendation = await ruleEngineService.evaluate({
     health:     healthScore,
     trend:      features.trend_score,
     derivative: features.derivative_score,
     volume:     features.volume_score,
     momentum:   features.momentum_score,
     confidence: features.confidence_score,
   }, activeVersion.id);

3. Replace old snapshot với normalized:
   await snapshotService.createDailySnapshot(
     date, coinScoresList, narrativeScoresList, activeVersion.id
   );

4. Add feature provenance:
   const provenance = buildProvenance(sources, indicators, calculatedAt);
   // Save to features.source_provenance

═══════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════

□ src/lib/services/indicator.service.ts
□ src/lib/services/rule-engine.service.ts
□ src/lib/services/snapshot.service.ts
□ Refresh pipeline updated (3 points above)
□ Unit tests cho rule engine (AND/OR/priority/fallback/missing field)
□ Unit tests cho indicator service (upsert behavior)
□ Unit tests cho snapshot service (create + query)
□ npx tsc --noEmit → 0 errors
□ Report: "B-P1-DONE"
```

---

## Agent C-P1: `C_P1_APIs.md`

```markdown
# Agent C-P1: API Routes P1
# Depends on: A-P1-DONE + B-P1-DONE

═══════════════════════════════════════════════════════════
ENDPOINTS TO CREATE
═══════════════════════════════════════════════════════════

1. GET /api/indicators/[coinId]
   Params: date (YYYY-MM-DD), timeframe (1d/4h/1h/15m)
   → { success, data: IndicatorRecord[] }

2. GET /api/indicators/[coinId]/history
   Params: days (default 30), type (indicator_type string)
   → { success, data: Array<{date, value}> }

3. GET /api/admin/recommendation-rules
   → { success, data: RecommendationRule[] }

4. POST /api/admin/recommendation-rules
   Body: { priority, signal, logicOperator, conditions, reasonTemplate }
   Validate:
     - priority: number > 0
     - signal: one of STRONG_WATCH/WATCH/OBSERVE/CAUTION/WEAK
     - logicOperator: 'AND' | 'OR'
     - conditions: array with at least 1 item
     - each condition has field/operator/value
   → 201 { success, data: RecommendationRule }
   → 422 if validation fails

5. PUT /api/admin/recommendation-rules/[id]
   → 200 { success, data: updated rule }
   → 404 if not found

6. DELETE /api/admin/recommendation-rules/[id]
   (soft delete: sets is_active = false)
   → 200 { success, data: { deactivated: true } }

7. GET /api/snapshots
   Params: limit (default 30, max 90)
   → { success, data: SnapshotSummary[] }

8. GET /api/snapshots/[date]/coins
   Date format: YYYY-MM-DD
   → { success, data: CoinSnapshotPoint[] }

9. GET /api/snapshots/[date]/narratives
   → { success, data: NarrativeSnapshotPoint[] }

═══════════════════════════════════════════════════════════
VALIDATION RULES
═══════════════════════════════════════════════════════════

Rule conditions validation:
  VALID fields: health, trend, derivative, volume, momentum, confidence
  VALID operators: >, >=, <, <=, ==, !=
  VALID value: number between 0 and 100

Signal validation:
  VALID: STRONG_WATCH, WATCH, OBSERVE, CAUTION, WEAK

Date validation:
  Must be YYYY-MM-DD format
  Must be valid calendar date
  Must not be in future

═══════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════

□ 9 route files created
□ All validation implemented
□ Manual tests cho tất cả endpoints
□ Error cases tested (bad input, not found, etc.)
□ Report: "C-P1-DONE"
```

---

## Agent D-P1: `D_P1_Frontend.md`

```markdown
# Agent D-P1: Frontend UI P1
# Depends on: C-P1-DONE

═══════════════════════════════════════════════════════════
TASK 1: Rule Engine Admin UI
═══════════════════════════════════════════════════════════

Thêm "Rules" sub-tab trong Admin → Rule Versions tab:

RulesEditor component:
  - List all rules cho active version
  - Hiển thị: priority, signal, logic, conditions count, reason template
  - Sort by priority DESC
  - Toggle active/inactive
  - "Add Rule" button → modal form

AddRuleModal:
  Fields:
    - Priority (number input)
    - Signal (select: STRONG_WATCH/WATCH/OBSERVE/CAUTION/WEAK)
    - Logic (radio: AND / OR)
    - Conditions (dynamic list):
        + Add Condition button
        + Each condition: [field dropdown] [operator dropdown] [value input]
    - Reason Template (textarea, show {health} {trend} etc. as hints)
  
  Validation:
    - Priority > 0
    - At least 1 condition
    - All condition fields filled

RuleCard component:
  Display:
    Priority: [100]  Signal: [STRONG_WATCH]  Logic: [AND]
    
    Conditions:
      ✓ health >= 85
      ✓ trend >= 75
      ✓ derivative >= 70
      ✓ confidence >= 60
    
    Reason: "Strong health ({health}) with solid trend..."
    
    [Edit] [Deactivate]

═══════════════════════════════════════════════════════════
TASK 2: Indicator Detail Panel
═══════════════════════════════════════════════════════════

Thêm vào Coin Detail page:

IndicatorPanel component:
  Title: "Indicator Values (1D)"
  
  Grid layout (2 columns):
    Trend Indicators:
      EMA 9:   [value]  [distance from price %]
      EMA 21:  [value]
      EMA 50:  [value]
      EMA 200: [value]
      ADX 14:  [value]  [label: Strong/Weak Trend]
    
    Momentum:
      RSI 14:  [value]  [overbought/oversold badge]
      MACD:    [value]  [signal, histogram]
    
    Volatility:
      BB 20:   [%B value]  [Upper/Lower band]
      ATR 14:  [value]
    
    Volume:
      VWAP:    [value]  [above/below label]

Fetch from: GET /api/indicators/[coinId]?date=today&timeframe=1d

═══════════════════════════════════════════════════════════
TASK 3: Snapshot History Page
═══════════════════════════════════════════════════════════

Page MỚI: src/app/snapshots/page.tsx

Layout:
  Header: "Morning Snapshot History"
  
  Date selector (last 30 days dropdown)
  
  When date selected:
    Narrative Summary cards (health score, change, top coin)
    Coin table (symbol, health, signal, change)

Fetch:
  GET /api/snapshots?limit=30 → populate date dropdown
  GET /api/snapshots/[date]/narratives → narrative cards
  GET /api/snapshots/[date]/coins → coin table

═══════════════════════════════════════════════════════════
TASK 4: P1E - Fix analyzer.ts imports
═══════════════════════════════════════════════════════════

File: src/lib/technical-analysis/analyzer.ts

FIND:
  import {
    KlineData,
    TechnicalAnalysisResult,
    Timeframe,
    SignalType,
    TIMEFRAME_WEIGHTS,
    TimeframeResult,
  } from "./types";

REPLACE WITH:
  import {
    KlineData,
    TechnicalAnalysisResult,
    Timeframe,
    SignalType,
    DetailedSignal,
    Direction,
    TIMEFRAME_WEIGHTS,
    TimeframeResult,
  } from "./types";

FIND (in applyAdjustments):
  const directions: Record<string, string> = Object.fromEntries(

REPLACE:
  const dirs: Record<string, Direction> = Object.fromEntries(

FIND (collect directions):
  const directions: Record<string, string> = {};

REPLACE:
  const directions: Record<string, DetailedSignal> = {};

Verify: npx tsc --noEmit → 0 errors

═══════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════

□ Admin Rule Engine UI (list + add + toggle)
□ AddRuleModal with dynamic conditions
□ Indicator Panel trên Coin Detail
□ Snapshot History page (/snapshots)
□ analyzer.ts import fix
□ No TypeScript errors
□ No console errors in browser
□ Report: "D-P1-DONE"
```

---

# 📄 03_Review_Pack_P1.md

```markdown
# P1 Review Pack
# Run before merging P1 branch

═══════════════════════════════════════════════════════════
PRE-REVIEW COMMANDS
═══════════════════════════════════════════════════════════

npx tsc --noEmit        # 0 errors
npm run build           # success
npm test                # all pass
npx drizzle-kit push --dry-run  # no destructive ops

═══════════════════════════════════════════════════════════
DATABASE REVIEW
═══════════════════════════════════════════════════════════

□ indicators table exists với UNIQUE constraint
□ recommendation_rules table exists
□ 6 default rules seeded cho version 1
□ morning_snapshot_headers/coins/narratives tables exist
□ features.source_provenance column added
□ All indexes created
□ Rollback script exists

═══════════════════════════════════════════════════════════
INDICATOR ENGINE REVIEW
═══════════════════════════════════════════════════════════

□ calculateAndSave uses UPSERT (not plain INSERT)
□ All 10 indicator types calculated
□ MACD saves meta (signal + histogram)
□ Source field populated correctly
□ No duplicate indicators per date/timeframe/type

═══════════════════════════════════════════════════════════
RULE ENGINE REVIEW
═══════════════════════════════════════════════════════════

□ AND logic: ALL conditions required
□ OR logic: ANY condition sufficient
□ Priority sorted DESC before evaluation
□ First matching rule wins (no double-matching)
□ Missing field returns false (NOT throw)
□ Fallback OBSERVE when no rule matches
□ reasonTemplate uses correct format tokens
□ Deactivated rules are skipped

Test specific scenarios:
□ health=87, trend=78, deriv=72, conf=65 → STRONG_WATCH
□ health=68, deriv=87, trend=71 → WATCH (rule priority 80)
□ health=45 → WEAK (priority 10)
□ health=50, confidence=25 → CAUTION (OR rule)

═══════════════════════════════════════════════════════════
SNAPSHOT NORMALIZATION REVIEW
═══════════════════════════════════════════════════════════

□ Header created once per date (unique constraint)
□ Coin records: one row per active coin per snapshot
□ Narrative records: one row per narrative per snapshot
□ Historical query returns ASC date order
□ Old morning_snapshots table still populated (backward compat)

═══════════════════════════════════════════════════════════
API REVIEW
═══════════════════════════════════════════════════════════

□ All 9 new endpoints respond correctly
□ Rule condition validation rejects invalid fields/operators
□ Signal validation rejects unknown signals
□ Date format validation works
□ Pagination/limit works for snapshot history

═══════════════════════════════════════════════════════════
UI REVIEW
═══════════════════════════════════════════════════════════

□ Rule Engine tab in Admin works
□ AddRuleModal validates before submit
□ Dynamic conditions add/remove works
□ Indicator Panel shows values
□ Snapshot History page loads
□ analyzer.ts imports fixed (no TS errors)

═══════════════════════════════════════════════════════════
REGRESSION
═══════════════════════════════════════════════════════════

□ Dashboard loads correctly
□ Refresh still works end-to-end
□ Existing recommendations still generated
□ Health scores unchanged in calculation

═══════════════════════════════════════════════════════════
SIGN-OFF
═══════════════════════════════════════════════════════════

Reviewed by: _______________  Date: _______________
P1A Indicator Table:    [ ] PASS  [ ] FAIL
P1B Rule Engine:        [ ] PASS  [ ] FAIL
P1C Snapshot Normal:    [ ] PASS  [ ] FAIL
P1D Feature Provenance: [ ] PASS  [ ] FAIL
P1E Import Fix:         [ ] PASS  [ ] FAIL

Approved to merge: [ ] YES  [ ] NO
```

---

# 📄 04_Verification_Pack_P1P2.md

```markdown
# P1 + P2 Verification Pack
# Run after deployment

═══════════════════════════════════════════════════════════
P1 VERIFICATION
═══════════════════════════════════════════════════════════

-- 1. Indicators populated after refresh
SELECT coin_id, date, timeframe, indicator_type, indicator_value
FROM indicators
WHERE date = CURRENT_DATE
ORDER BY coin_id, indicator_type
LIMIT 20;
-- Expected: rows with EMA/RSI/MACD etc. for each coin

-- 2. Rules seeded correctly
SELECT priority, signal, logic_operator,
       jsonb_array_length(conditions) as condition_count
FROM recommendation_rules
ORDER BY priority DESC;
-- Expected: 6 rows, priorities 100/90/80/70/60/10

-- 3. Normalized snapshots created
SELECT msh.date, msh.total_coins,
       COUNT(msc.id) as coin_records,
       COUNT(msn.id) as narrative_records
FROM morning_snapshot_headers msh
LEFT JOIN morning_snapshot_coins msc ON msc.snapshot_id = msh.id
LEFT JOIN morning_snapshot_narratives msn ON msn.snapshot_id = msh.id
GROUP BY msh.id, msh.date
ORDER BY msh.date DESC
LIMIT 5;
-- Expected: coin_records matches total_coins

-- 4. API smoke tests
curl -s "http://localhost:3000/api/indicators/1?date=$(date +%Y-%m-%d)&timeframe=1d" | jq '.success'
# Expected: true

curl -s "http://localhost:3000/api/admin/recommendation-rules" | jq '.data | length'
# Expected: 6

curl -s "http://localhost:3000/api/snapshots?limit=5" | jq '.success'
# Expected: true

-- 5. Rule Engine fires correctly
curl -s "http://localhost:3000/api/coins/1" | jq '.data.recommendation'
# Expected: signal is one of STRONG_WATCH/WATCH/OBSERVE/CAUTION/WEAK

═══════════════════════════════════════════════════════════
P2 VERIFICATION (after P2 deploy)
═══════════════════════════════════════════════════════════

-- 1. Decision signals table populated
SELECT coin_id, date, base_health, event_risk_score,
       adjusted_score, adjustment_reason
FROM decision_signals
WHERE date = CURRENT_DATE
LIMIT 5;

-- 2. Create test event, verify score adjusts
curl -X POST http://localhost:3000/api/admin/events \
  -H "Content-Type: application/json" \
  -d '{
    "coinId": 1,
    "eventType": "TOKEN_UNLOCK",
    "eventDate": "'"$(date +%Y-%m-%d)"'",
    "riskLevel": "HIGH",
    "riskScore": 70,
    "title": "Test unlock event",
    "expiresAt": "'"$(date -d '+7 days' +%Y-%m-%d)"'"
  }'

# Run refresh
curl -X POST http://localhost:3000/api/refresh

# Check adjusted score
curl -s "http://localhost:3000/api/coins/1/decision" | \
  jq '{base: .data.baseHealth, adjusted: .data.adjustedScore, reason: .data.adjustmentReason}'
# Expected: adjustedScore < baseHealth by ~15-25 points

-- 3. Correlation calculated
curl -s "http://localhost:3000/api/narratives/1/correlations" | jq '.success'
# Expected: true, data contains matrix

-- 4. Alerts work
curl -X POST http://localhost:3000/api/admin/alerts/rules \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test alert",
    "scope": "coin",
    "scopeId": 1,
    "triggerType": "SCORE_BELOW",
    "triggerValue": 95
  }'

# Run refresh, check if alert fired
curl -s "http://localhost:3000/api/admin/alerts/history?limit=5" | jq '.data'

═══════════════════════════════════════════════════════════
FINAL SIGN-OFF
═══════════════════════════════════════════════════════════

P1 Verification:
  Indicators populated:         [ ] PASS  [ ] FAIL
  Rules seeded + working:       [ ] PASS  [ ] FAIL
  Snapshot normalized:          [ ] PASS  [ ] FAIL
  All APIs respond:             [ ] PASS  [ ] FAIL
  UI components visible:        [ ] PASS  [ ] FAIL
  No regression:                [ ] PASS  [ ] FAIL

P2 Verification:
  Decision Engine adjusts:      [ ] PASS  [ ] FAIL
  Event Risk flows correctly:   [ ] PASS  [ ] FAIL
  Correlations calculated:      [ ] PASS  [ ] FAIL
  Alerts trigger correctly:     [ ] PASS  [ ] FAIL
  Admin Analytics shows data:   [ ] PASS  [ ] FAIL

Overall P1: [ ] VERIFIED  [ ] ISSUES
Overall P2: [ ] VERIFIED  [ ] ISSUES

Verified by: _______________  Date: _______________
```

---

## Summary: Full Roadmap

```
╔══════════════════════════════════════════════════════════════╗
║                  FULL ROADMAP SUMMARY                        ║
╠══════════╦══════════════════════════════╦═══════════════════╣
║ Sprint   ║ Key Deliverables             ║ Status            ║
╠══════════╬══════════════════════════════╬═══════════════════╣
║ P0       ║ Weighted Narrative Health    ║ ✅ DONE           ║
║          ║ Rule Version Tracking        ║                   ║
║          ║ Health Timeline UI           ║                   ║
║          ║ TA Engine Bug Fixes          ║                   ║
╠══════════╬══════════════════════════════╬═══════════════════╣
║ P1       ║ Indicator Table              ║ 🔵 READY TO START ║
║          ║ Multi-condition Rule Engine  ║                   ║
║          ║ Snapshot Normalization       ║                   ║
║          ║ Feature Provenance           ║                   ║
╠══════════╬══════════════════════════════╬═══════════════════╣
║ P2       ║ Decision Engine              ║ 📋 PLANNED        ║
║          ║ Event Risk Framework         ║                   ║
║          ║ Correlation Analysis         ║                   ║
║          ║ Narrative Momentum           ║                   ║
║          ║ Alert System                 ║                   ║
╠══════════╬══════════════════════════════╬═══════════════════╣
║ Phase 2  ║ AI Summary                   ║ 🔮 FUTURE         ║
║          ║ Telegram Bot                 ║                   ║
║          ║ Whale Tracking               ║                   ║
║          ║ On-chain Analysis            ║                   ║
╠══════════╬══════════════════════════════╬═══════════════════╣
║ Phase 3  ║ ML Integration               ║ 🔮 FUTURE         ║
║          ║ Backtest Engine              ║                   ║
║          ║ Portfolio Management         ║                   ║
╚══════════╩══════════════════════════════╩═══════════════════╝

Files to create for P1+P2:
  01_P1_Implementation_Pack.md   ← THIS FILE
  01_P2_Implementation_Pack.md   ← THIS FILE
  02_Agent_Work_Pack_P1/
    A_P1_Database.md
    B_P1_Services.md
    C_P1_APIs.md
    D_P1_Frontend.md
  02_Agent_Work_Pack_P2/         ← Generate when P1 done
    A_P2_Database.md
    B_P2_Decision_Engine.md
    C_P2_Events_Correlation.md
    D_P2_Alerts_Analytics.md
    E_P2_Frontend.md
  03_Review_Pack_P1.md
  04_Verification_Pack_P1P2.md
